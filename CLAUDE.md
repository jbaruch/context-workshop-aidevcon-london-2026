# Context Workshop — project rules

The mandatory checks below are the **rules** bucket of the decomposition: they
must hold regardless of which skill (if any) is driving the work, so they are
loaded here rather than buried inside a single skill. Each one leans on a
deterministic verifier in `scripts/`.

@rules/tests-first.md
@rules/merge-on-clean-review.md
@rules/docs-need-human-review.md
